{/*import axios from 'axios';

const API_BASE_URL = 'http://127.0.0.1:8000/docs#/Grid'; // غيري الـ IP لو السيرفر مختلف

export const saveGridLayout = async (newsletterId, slots) => {
    // slots لازم تكون array فيها {slot_number, article_id}
    const response = await axios.post(${API_BASE_URL}/save, {
        newsletter_id: newsletterId,
        slots: slots
    });
    return response.data;
};

export const getDashboardGrid = async (newsletterId) => {
    const response = await axios.get(${API_BASE_URL}/dashboard/${newsletterId});
    return response.data; // دي هترجع الـ 6 أخبار بتفاصيلهم
};*/}